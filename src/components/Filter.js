import React from 'react';

function Filter() {

  return (
    <div className="btn-group mb-3" role="group" aria-label="Basic example">
      <button onClick={() => {}} className="btn btn-secondary">Все</button>
      <button onClick={() => {}} className="btn btn-secondary">Активные</button>
      <button onClick={() => {}} className="btn btn-secondary">Выполненные</button>
    </div>
  );
}

export default Filter;
